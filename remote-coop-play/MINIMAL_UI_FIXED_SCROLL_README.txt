MINIMAL UI + SCROLL FIX

This version removes the large/cosmic direction and replaces it with a cleaner minimal UI.

Included:
- Minimal white/slate interface with blue accents
- Home, Host, Join and Settings still separated
- Server settings hidden in Settings modal
- Debug drawer collapsed by default
- Scroll fixed by removing body overflow blocking and giving panels proper overflow behavior
- Streaming, fullscreen, input, debug and signaling logic preserved

Rebuild:
1. Replace your repo files with this package.
2. Commit and push.
3. Run GitHub Actions build.
4. Download the new artifact.